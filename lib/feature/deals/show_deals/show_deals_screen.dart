import 'package:flutter/material.dart';

class ShowDealsScreen extends StatelessWidget {
  const ShowDealsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Show Deals'),
      ),
    );
  }
}
