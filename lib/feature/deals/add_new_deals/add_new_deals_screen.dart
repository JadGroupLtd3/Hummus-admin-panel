import 'package:flutter/material.dart';

class AddNewDealsScreen extends StatelessWidget {
  const AddNewDealsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add New Deals '),
      ),
    );
  }
}
