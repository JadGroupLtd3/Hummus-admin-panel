import 'package:flutter/material.dart';

class ShowHashtagsScreen extends StatelessWidget {
  const ShowHashtagsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Show Hashtags'),
      ),
    );
  }
}
