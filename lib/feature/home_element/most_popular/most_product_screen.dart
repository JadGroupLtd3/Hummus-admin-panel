import 'package:flutter/material.dart';

class MostProductScreen extends StatelessWidget {
  const MostProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Most Product Screen'),
      ),
    );
  }
}
