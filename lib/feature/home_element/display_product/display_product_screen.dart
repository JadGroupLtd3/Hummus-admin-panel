import 'package:flutter/material.dart';

class DisplayProductScreen extends StatelessWidget {
  const DisplayProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Display Product Screen'),
      ),
    );
  }
}
